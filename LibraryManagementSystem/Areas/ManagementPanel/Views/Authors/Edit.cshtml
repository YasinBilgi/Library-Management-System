@model LibraryManagementSystem.Entites.Models.Author

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/ManagementPanel/Views/Shared/_Layout.cshtml";
}

<h1>Düzenle</h1>

@using (Html.BeginForm("Edit", "Authors", FormMethod.Post, new {enctype = "multipart/form-data"}))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new {@class="text-danger"})
        @Html.HiddenFor(model => model.Id)

        <label class="control-label col-md-2">Adı</label>
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.FullName, new {htmlAttibutes = new {@class = "form-control"}})
                @Html.ValidationMessageFor(model => model.FullName, "", new {@Classs = "text-danger"})
            </div>
        </div>

        <label class="control-label col-md-2">Doğum Tarihi</label>
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.BirthDate, new {htmlAttibutes = new {@class = "form-control", @type="datetime"}})
                @Html.ValidationMessageFor(model => model.BirthDate, "", new {@Classs = "text-danger"})
            </div>
        </div>

        <label class="control-label col-md-2">Biyografi</label>
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.Biography, new {htmlAttibutes = new {@class = "form-control", @type="date"}})
                @Html.ValidationMessageFor(model => model.Biography, "", new {@Classs = "text-danger"})
            </div>
        </div>

        <label class="control-label col-md-2">Resim</label>
        @*İmage*@
        <div class="form-group">
            @*  @Html.LabelFor(model => model.İmageUrl, htmlAttributes: new {@class = "control-label col-md-2"})*@

            <div class="col-md-10">
                <input type="file" accept="image/*" name="img" id="img" />
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.İsActive, htmlAttributes: new {@class = "control-label col-md-2"})

            <div class="col-md-10">
                @Html.DropDownListFor(model => model.İsActive, new SelectList(
            new[]{
            new{Value = "true", Text = "Aktif"},
            new{Value = "false", Text = "Pasif"}
            },
            "Value",
            "Text",
            Model.İsActive), new{@class = "form-control"})
            </div>
            @Html.ValidationMessageFor(model => model.İsActive, "",
        new {@class = "text-danger"})
        </div>

        <br />
        <input type="submit" value="Kaydet" class="btn btn-primary" />
    </div>
}

